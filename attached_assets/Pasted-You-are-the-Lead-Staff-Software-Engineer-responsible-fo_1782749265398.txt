You are the Lead Staff Software Engineer responsible for stabilizing and production-hardening Earth Guardian AI (Raksh AI).

Your goal is to make the AI backend production-ready, deployment-safe, fault-tolerant, and maintainable without changing the UI or removing any existing features.

==============================
CRITICAL RULES
==============

* Do NOT rewrite the project.
* Do NOT redesign the architecture.
* Do NOT modify the UI/UX.
* Do NOT remove existing functionality.
* Do NOT hardcode any API keys.
* Preserve all current features.
* Fix only the root causes.
* Make minimal, production-grade changes.
* Verify every fix before moving to the next step.

==============================
PHASE 1 — COMPLETE PROJECT AUDIT
================================

Audit the complete AI pipeline:

Frontend
→ API Routes
→ Router
→ AI Provider Manager
→ Gemini Client
→ Response
→ Frontend Rendering

Find every issue related to:

* Environment variables
* API initialization
* Duplicate Gemini clients
* Multiple provider managers
* Singleton misuse
* Provider fallback
* Streaming
* Timeouts
* Loading loops
* Error handling
* Deployment
* Replit Secrets
* Startup initialization
* AI request lifecycle
* Image generation
* Vision
* Voice
* Document analysis

Before modifying anything report:

* Root causes
* Files affected
* Fix plan

==============================
PHASE 2 — PERMANENT GEMINI INITIALIZATION
=========================================

Create ONE centralized AI Provider Manager.

Requirements:

* Load GEMINI_API_KEY ONLY from:

process.env.GEMINI_API_KEY

* Never read keys from anywhere else.
* Never recreate Gemini clients.
* Never initialize Gemini inside request handlers.
* Initialize Gemini exactly once during server startup.
* Export a singleton Gemini client.
* Every request must reuse the singleton.
* Never cache invalid clients.

Startup logs must display:

✓ Environment loaded

✓ GEMINI_API_KEY detected

✓ Gemini initialized

✓ Model loaded

✓ AI Provider Ready

✓ Raksh AI Ready

==============================
PHASE 3 — DEPLOYMENT SAFE
=========================

The project must work correctly after:

* Git clone
* GitHub pull
* Replit import
* Replit deployment
* Server restart
* New deployment

WITHOUT modifying any source code.

The ONLY manual deployment step allowed should be:

Add GEMINI_API_KEY once inside Replit Secrets.

After that:

* No code changes.
* No configuration edits.
* No environment fixes.
* No reinitialization.

The application must automatically detect the key during startup.

If the key is missing:

Return:

Configuration Error:
GEMINI_API_KEY not found in Replit Secrets.

Never hang.

Never retry forever.

Never create invalid clients.

Never leave frontend loading forever.

==============================
IMPORTANT
=========

I understand that Replit Secrets are isolated per account.

This project CANNOT permanently copy secrets across different Replit accounts.

Instead:

Design the application so that whenever this repository is imported into ANY Replit account, the ONLY setup step required is adding GEMINI_API_KEY once in Replit Secrets.

Everything else must work automatically.

==============================
PHASE 4 — REQUEST STABILITY
===========================

Every AI request must:

* Timeout after 30 seconds.
* Always return success or error.
* Never freeze.
* Never leave loading forever.
* Never throw uncaught exceptions.

Gracefully handle:

401

403

404

408

429

500

503

Network failures

Quota exceeded

Invalid responses

Stream interruptions

If quota exceeded:

Return a friendly error.

Keep application usable.

==============================
PHASE 5 — PROVIDER HEALTH
=========================

Implement:

GET /api/raksh/status

Return:

* keyLoaded
* initialized
* healthy
* provider
* model
* uptime
* lastInitialization
* version

==============================
PHASE 6 — REMOVE DUPLICATE LOGIC
================================

Remove:

* Duplicate Gemini initialization
* Duplicate provider managers
* Duplicate env loading
* Duplicate timeout logic
* Duplicate retry logic
* Dead AI services
* Unused AI code
* Stale initialization paths

==============================
PHASE 7 — FULL VERIFICATION
===========================

Automatically verify:

✓ Chat

✓ Image Generation

✓ Vision

✓ Voice

✓ Document Analysis

✓ Streaming

✓ Status endpoint

✓ Startup logs

✓ Singleton reuse

✓ No duplicate clients

✓ No infinite loading

✓ No hanging requests

✓ No initialization race conditions

✓ Server restart

✓ Deployment

==============================
PHASE 8 — CODE QUALITY
======================

Improve only where necessary.

Ensure:

* Clean architecture
* Proper logging
* Proper error handling
* Reusable services
* Modular provider manager
* Production-grade code
* No unnecessary complexity

==============================
FINAL REPORT
============

Return ONLY:

1. Root causes found

2. Files modified

3. Exact fixes applied

4. Verification results

5. Remaining issues

Do NOT stop until the backend is stable, deployment-safe, and production-ready.
